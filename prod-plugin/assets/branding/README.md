Place the bundled official MLE TM logo here as `MLETM.png`.

PROD will use this as the offline fallback. Authorized production staff can override it at runtime with `Overlay/branding/MLETM.png` without rebuilding the plugin.
